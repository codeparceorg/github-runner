name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [docker, vm, kubernetes]

    steps:
      - name: Test
        run: |
          echo "Runner funcionando"
          hostname
          whoami
          sleep 120