name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: devops

    steps:
      - name: Test
        run: |
          echo "Runner funcionando"
          hostname
          whoami
          docker --version